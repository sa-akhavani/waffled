spring does not support &writer; but doesnt care if we have docktype if not used
gin does not support BOOK. doesnt throw error on &writer; but cannot parse it
fastapi doesnt work properly after receiving &writer (no semicolon) and needs to be restarted! but works if used properly
